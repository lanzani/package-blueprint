[project]
name = "{{package_name}}"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Author", email = "author@email.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit",
    "ruff",
]
